import SignatureGenerator from 'src/components/SignatureGenerator';

# Signature Generator

This tool helps you simulate signature generation with `checksumKey` and `data`.

See details in [Verify data with signature](/developers/signature).

<SignatureGenerator />
